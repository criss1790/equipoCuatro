<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_pico_botella"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_pico_botella_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pico_Botella"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".view.MainActivity"
            android:exported="false" />

        <activity
            android:name=".view.RetosActivity"
            android:exported="false" />

        <activity
            android:name=".view.InstruccionesActivity"
            android:exported="false" />

        <activity
            android:name=".view.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>